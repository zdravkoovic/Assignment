<UserControl x:Class="Assignment.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Assignment.Views" 
             mc:Ignorable="d" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"></ColumnDefinition>
            <ColumnDefinition Width="300"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="250"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Grid.Column="0" Grid.Row="0" Content="Loaders" Margin="5" Command="{Binding LoadersCommand}"></Button>
        <Button Grid.Column="1" Grid.Row="0" Content="To-Do List" Margin="5" Command="{Binding ToDoListCommand}"></Button>

        <ContentControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" x:Name="ActiveItem"/>
    </Grid>
</UserControl>
