<UserControl x:Class="Assignment.Views.ToDoSubmitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Assignment.Views"
             mc:Ignorable="d" >
    <Grid>
        <StackPanel Orientation="Vertical">
            <Label Content="Name"></Label>
            <TextBox Text="{Binding Path=ItemName, Mode=OneWayToSource}"></TextBox>
            <Label Content="Priority"></Label>
            <ComboBox ItemsSource="{Binding Priorities}" SelectedValue="{Binding Path=SelectedPriority}"/>
            <Button Content="Submit" Margin="10" Command="{Binding SubmitCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
